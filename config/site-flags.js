/**
 * Drapeaux d'affichage du site SPLICE.
 *
 * Ce fichier n'est jamais publié (voir _config.yml, section "exclude").
 * Après toute modification d'un drapeau, appliquer avec :
 *
 *     node scripts/sme-packages.mjs
 *
 * Le contenu masqué n'est jamais supprimé : il est archivé verbatim
 * dans _masked/sme-packages/ et restauré par le même script.
 */

// Masqué le 20/09/2026. SPLICE ne peut pas être prestataire
// SME Packages avant d'avoir un an d'existence. À réévaluer
// à l'automne 2027 après confirmation par la House of
// Entrepreneurship.
export const SHOW_SME_PACKAGES = false;
